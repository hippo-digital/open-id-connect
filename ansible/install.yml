---

# Required infrastructure
# -----------------------

- hosts: all
  user: user1
  roles:
   - base
   - idp
   - iptables
   - ssh
  vars:
    - enable_sshd_timeout: false
    - iptables_inbound_ports:
      - { protocol: 'tcp', port: 443 }


